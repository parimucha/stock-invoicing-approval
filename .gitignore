node_modules/
.env
.env.local
.env.*.local
.DS_Store
*.log

# Local ingestion output — contains employee time entries and internal JIRA metadata.
data/

# Vercel CLI link dir — contains .env.production (secrets). Never commit.
.vercel/
